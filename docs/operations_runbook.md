# Operations Runbook

1. Configure `ENTSOE_API_KEY` locally.
2. Run `pytest`.
3. Run local Bronze, Silver, Gold, or production pipeline commands through the CLI.
4. Review `ops.pipeline_runs` and `dq.dq_stats` outputs after each run.
